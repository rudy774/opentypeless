name: Disabled legacy Windows SignPath workflow

on:
  workflow_dispatch:

permissions: {}

jobs:
  disabled:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - name: Legacy workflow is intentionally unavailable
        run: echo "SignPath publishing is unsupported. Use the reviewed release.yml workflow."
